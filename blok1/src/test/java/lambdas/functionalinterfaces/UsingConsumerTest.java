package lambdas.functionalinterfaces;


import org.junit.jupiter.api.Test;

import java.util.Arrays;
import java.util.List;


public class UsingConsumerTest {

 /*   @Test
    public void printIntegers() {
        List<Integer> list = Arrays.asList(1, 2, 3, 4, 5);
//        forEach(list, (Integer i) -> System.out.println(i));
//        forEach(list, s -> list.add(s)); // void compatibility: we can pass in a Predicate (lambda returns boolean) as a Consumer! (Boolean) Statement expression is compatible with void.
        labs.lambda.functionalinterfaces.UsingConsumer.forEach(list, integer -> list.add(integer)); // void compatibility: we can pass in a Predicate (lambda returns boolean) as a Consumer! (Boolean) Statement expression is compatible with void.
    }*/

}
